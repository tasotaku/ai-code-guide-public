# Changelog

このファイルは AI Code Guide のバージョンごとの変更点を記録します。
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います（新機能=minor / バグ修正=patch）。

配布は研究室内向けに GitHub Releases で行っています。更新方法は各リリースの手順に従ってください（自動更新はされないため、新しい vsix を手動で入れ直します）。

## [Unreleased] - 2026-08-22

### 変更
- Codexの標準表示を、会話内のMCP Appカードから広いブラウザWebviewへ変更しました
- VS Codeの標準タブと同じく、クラス・メソッド・関数・定数を定義順の縦カードで表示します。クラス配下の開閉やカード内展開も同じ操作です
- 左のコードと右のカード一覧を常に同時表示し、選んだカードのコード範囲を色で対応づけます。関数の目的・入力・出力、クラスの役割・状態・主な機能、補足、読む順番ごとの処理ブロックはカード直下へ展開されます
- VS Codeのコード地点へ移動する操作と、VS Code側の標準表示への同期は引き続き利用できます
- Codex標準Webviewの各カードにあった「VS Code」ボタンを撤去し、カード背景色を初期表示から濃くしました。詳細を1件開いても、他カードの背景色は変化しません
- Codex標準Webviewの背景を独自の24%単層塗りから、VS Code標準タブと同じ通常15%・クラス見出し22%・クラス本体8%・展開12%・継ぎ目32%の積層配色へ変更しました
- 詳細展開後の完了メッセージは2.4秒で自動的に消え、表示中もカード操作を遮りません。エラーメッセージも6秒で消えます
- クラス選択時はコード範囲全体を濃く塗らず、カードの水色枠とコード左端の色線だけで選択位置を示します。関数・メソッドの範囲強調は維持します
- 左側コードの通常背景を9%から12%へ濃くし、選択していないクラス・関数も色を判別しやすくしました。選択したメソッド／関数は20%のままです

## [0.27.18] - 2026-08-08

### 変更
- Codexで表示する機能ごとに、用途に合う表示面を使い分けるようにしました
  - コード地点一覧、概要、構成、コード図、インライン解説は従来どおり会話内に表示します
  - 実行トレースだけはCodex内の広いブラウザパネルへ開き、会話内の表示が消えないようにしました
- MCP Appの「全画面で見る」は、実際の挙動に合わせて「右パネルで開く」へ改めました

### 修正
- 実行トレースを左右独立の横スクロール、36px固定行高、折り返しなしで表示します
- 長いコードや実行値でも左右の行対応がずれないようにしました
- 古い表示URL、workspace外コード移動、不正な行番号を安全に拒否し、再表示方法を案内します

## [0.27.9] - 2026-08-04

### 修正
- 外部AI（Codex・Claude等）が自分の確認作業のためにAI Code Guideを呼ばないようにしました
  - MCPの説明文を「人がコードを見たい・開きたい時だけ使う」に改め、AI自身がコードを読む・自分の実装を確認する目的での利用を禁止しました
- 外部AIからの表示要求でVS Codeが勝手に最前面へ出る動作を止めました
  - サイドバー表示・エディタでのファイル表示のどちらもフォーカスを奪いません
  - 前面化は、会話内UIのコード地点ボタンを人が押した時だけです

## [0.27.8] - 2026-08-01

### 修正
- 会話内コード図のノードを押した時だけ、対象のVS Codeウィンドウを最前面へ出すようにしました
  - 図の生成・表示や通常のコード解説では前面を切り替えません
  - ノードクリック時は該当ファイル・行・標準カードを開いてから、その処理を担当したVS Codeウィンドウを前面化します

## [0.27.7] - 2026-08-01

### 修正
- 会話内・VS Codeサイドバー内のコード図からコード地点を開いても、VS CodeをOSの最前面へ切り替えないようにしました
  - 対象ファイル・行・標準カードへの移動は背後のVS Code内で行います
  - `focusWindow`、VS Codeのウィンドウフォーカスコマンド、macOSのアプリ起動による前面化をすべて撤去しました

## [0.27.6] - 2026-08-01

### 修正
- 会話内へコード図を表示しただけでVS Codeが自動的に最前面へ出る問題を直しました
  - 図の生成・表示中は会話アプリを前面のまま維持します
  - 図のコード地点を押した時だけ、対象のVS Codeウィンドウを最前面へ出します

## [0.27.5] - 2026-08-01

### 追加
- CodexやChatGPTなどの会話内に、質問から作ったコード図をそのまま表示できるようにしました
  - 図の各コード地点には、その場所の役割を常時表示します
  - コード地点を押すと、対応するVS Codeのファイル・行と標準カードへ移動します

### 修正
- 会話内コード図などからコード地点を開いたとき、対象のVS Codeウィンドウが別アプリの背後に残る問題を直しました
  - VS Codeを複数ウィンドウで開いていても、対象ファイルを含むウィンドウを選んで最前面へ出します

## [0.27.4] - 2026-08-01

### 修正
- Codexなどから別リポジトリのコード図を作ると、開いている別のVS Codeワークスペースを誤って解析することがある問題を直しました
  - 図生成では入口Pythonファイルの絶対パスを必須にし、そのファイルを含むVS Codeだけへ接続します
  - 入口なし・相対パスでは誤った図を作らず、入力エラーとして停止します
- 長いPythonファイルで、質問対象の関数がファイル後半にあると図から欠落する問題を直しました
- 質問で9個以上の実在関数を指定しても、8ノード上限で欠落しないよう最大16ノードまで拡張しました
- 図生成用LLMが未設定のとき、「関係が見つからない」と誤表示せず設定不足を案内するようにしました

## [0.27.3] - 2026-07-31

### 修正
- 図のノードから別ファイルへ移動したとき、標準タブの対応カードに付いた青い強調が再描画で消えることがある問題を直しました

## [0.27.2] - 2026-07-31

### 追加
- チャットの質問とAI返信を範囲選択してコピーできるようにし、各メッセージに「コピー」ボタンも追加しました

### 修正
- ホバー説明やAI返信など、外部からコピーした通常テキストをチャット入力欄へ貼り付けても文字が入らない問題を直しました
- 図のノードから標準タブへ移動したとき、対応するカードが最初から中央表示・強調されるようにしました

## [0.27.1] - 2026-07-31

### 修正
- 実行トレースの長い値が `…` で省略されたとき、右側のトレース文字へ直接マウスを合わせると全文を表示するようにしました
- 元コード行とトレース文字のホバーが同時に発火し、同じ全文が二重表示されないようにしました

## [0.27.0] - 2026-07-31

### 追加
- サイドバーに「図」タブを追加しました。コードについて知りたいことを自然言語で入力すると、質問に合わせて次の3種類から適切な図を生成します
  - 処理順: 実際の呼び出し方向や必要な分岐を表示
  - 読解順: コードを読む順番と、その順番で読む考え方を表示
  - 依存関係: 対象の機能が利用する関数・機能をツリー表示
- 図のノードを押すと、実在確認済みのファイル・関数へ移動できます
- 作成した図をワークスペースごとに履歴保存し、AIを再実行せず再表示できるようにしました

### 変更
- 狭いサイドバーでも横スクロールせず読めるよう、3種類それぞれを縦長の専用レイアウトにしました
- 図全体の概要と各ノードの説明を分離しました
  - 概要には、処理全体の流れ・読解順の考え方・依存機能の全体像を表示します
  - 各ノードには「〜するため」のような目的句ではなく、そのコード地点が行うことを完結した文で表示します
- 図生成には実コードの断片と解析済みの import 関係を渡し、名前だけから存在しない関係を作らないようにしました

### 修正
- 同じ質問文の履歴が複数あると、選んでいない履歴まで選択色になる問題を直しました
- 処理順の矢印で、縦棒と矢尻の中心がずれる問題を直しました

## [0.26.3] - 2026-07-31

### 修正
- 構成タブを最初に開いたとき、検索欄しか表示されず、質問を入力した後に初めてファイル構成・参照関係・ディレクトリ設計が出る問題を直しました
  - タブを開いた時点で一度だけ構成を解析し、入力なしでも全体像を表示します
- 読解案内の履歴が0件のときに「過去の案内」欄そのものが表示されない問題を直しました
  - まだ案内がない場合も、保存される場所と次回から残ることを表示します

## [0.26.2] - 2026-07-31

### 追加
- コード上で右クリックの「標準カードへ移動」または Cmd+Alt+M を使うと、カーソル位置に対応する関数・メソッド・クラスのカードへ移動できるようにしました
  - 畳まれたクラス内のメソッドも自動で開き、対象カードを中央に表示します

### 変更
- 標準カード・コード背景・フロー図の色を6色から12色へ増やし、隣り合うカードを見分けやすくしました

## [0.26.1] - 2026-07-30

### 修正
- 構成タブの「理解したい処理を探す」で、日本語入力の変換確定に Enter を押すと検索が送信される問題を直しました

## [0.26.0] - 2026-07-30

### 追加
- 構成タブで、知りたい処理を自然言語で入力すると、読むべきファイル・関数を順番に案内できるようにしました
  - 案内は実際に存在するファイルと関数だけを表示します
  - 各地点から、標準表示・フローチャート・インライン解説・実行トレースを開けます

### 修正
- 幅の狭いサイドバーでも、読解案内の操作ボタンが画面外に押し出されないようにしました

## [0.25.1] - 2026-07-28

### 修正
- `BuyItem` のようにメソッドを持たないクラスで、`概要 ▶` を押すとコードの背景色だけが変わり、役割・状態・主な機能が表示されない問題を直しました

## [0.25.0] - 2026-07-28

### 追加
- 標準タブでクラスを開いたときに、そのクラスの「役割」「状態（保持する値・依存先）」「主な機能」を表示するようにしました。関数の「目的・入力・出力」と同じ位置に出ます

### 修正
- 子メソッド一覧を畳む三角とクラスの説明を開く操作が見分けにくかったため、クラスの説明側を右端の `概要 ▶/▼` ボタンに変更しました

## [0.24.2] - 2026-07-25

### 変更
- 複数の関数をまとめてトレースするときの実行を速くしました。これまでは同時に3つずつ、その3つが全部終わるまで次に進まない作り（1つ遅いものがあると残りが待たされる）でしたが、常に8つ走らせ、1つ終わるたびに次を始めるようにしました
  - 手元の計測では、8関数のファイルで約120秒 → 約40〜50秒

## [0.24.1] - 2026-07-25

### 修正
- サイドバー上部のタブが幅に収まらないときに出る「⋯」ボタンを押しても、隠れたタブの一覧が出ませんでした。メニューがタブ列の枠に切り取られて見えなくなっていたのを直しました

## [0.24.0] - 2026-07-25

### 変更
- 設定で選べるモデルを最新世代に入れ替えました（一覧が古く、旧世代のモデルしか選べませんでした）
  - Claude: Haiku 4.5（低コスト・高速）/ Sonnet 5（バランス）/ Opus 5（最高品質）
  - GPT: 5.6 Luna（低コスト・高速）/ 5.6 Terra（バランス）/ 5.6 Sol（最高品質）
  - Gemini: 3.5 Flash-Lite（低コスト・高速）/ 3.6 Flash（高品質）
- 初期設定のモデルを変更しました。設定を変えていない場合は、会話系・インライン解説／実行トレースが Claude Sonnet 5 になります（構造系は Haiku 4.5 のまま）
- 右下に出るトークン料金の目安を、新しいモデルの価格に合わせました
- Gemini 3系で「じっくり考える」の指定方法が変わったのに追随しました（3系は既定が深い思考のため、「速い」を選んだときは明示的に浅く指定します）

## [0.23.1] - 2026-07-25

### 修正
- 設定画面のモデル選択で、実行トレースがどの設定で切り替わるのか分からなかったのを直しました
  - 「インライン解説」の行を「インライン解説／実行トレース」に変更（実行トレースはこの設定のモデルを使っています）

## [0.23.0] - 2026-07-25

### 追加
- 実行トレースで、複数の関数をまとめて実行できるようにしました（1つずつカーソルを置いて実行する手間をなくすため）
  - サイドバーの「トレース」タブに、そのファイルの関数の一覧（チェックボックス）が出ます。既定は全部チェック済みで、「全選択」「全解除」もあります
  - 「選んだ関数をトレース」を押すと、チェックした関数を順に実行し、それぞれの行に変数の実値が**同時に**表示されます
  - 副作用があって実行を断った関数・実行に失敗した関数はとばして、残りを表示します（理由はまとめて通知します）
  - ループの周回（1周目・2周目…）は関数ごとに独立して切り替えられます。サイドバーの「ループの周回」とステータスバーには、どの関数のループかが分かるよう関数名が出ます
  - 「別の入力例で再実行」は、表示中の関数すべてを作り直します
  - 一覧に出るのはファイル直下の関数だけです（クラスのメソッドは今の実行方法では呼び出せないため出ません）

## [0.22.2] - 2026-07-25

### 修正
- サイドバーの「トレース」タブで、ボタンやキーの右にある説明文が右半分に押し込まれて何行にも折り返っていたのを、左列を狭くして読みやすくしました

## [0.22.1] - 2026-07-25

### 修正
- 周回切り替えのステータスバー表示が他の拡張の項目に押し出されて見えないことがあったので、画面左下の端に固定し、表示も「トレース 3周目/全4周」と名前付きにしました
- サイドバーの「トレース」タブに「ショートカット」の節を追加し、⌥⌘T（トレース実行）・⌥⌘C（解説表示に戻る）・⌥⌘←/⌥⌘→（前後の周回）を書きました

## [0.22.0] - 2026-07-25

### 追加
- 実行トレースの周回（1周目・2周目…）をボタンで切り替えられるようにしました。置き場所は2つで、どちらもクリック1回で動きます
  - 画面下のステータスバー: `◀ 2周目/全4周 ▶`。カーソルのあるループが対象です（キーボードの ⌥⌘←/→ と同じ対象）
  - サイドバーの「トレース」タブ: ループの一覧が出て、行ごとに ◀ ▶ が付きます。二重ループでも内側・外側を別々に動かせます
- 従来の ⌥⌘←/→ とループ行にホバーして出るリンクもそのまま使えます

### 修正
- ブロックの解説の上に、中身が空の行が1行入っていた問題を直しました（状態ボタン（読んだ/後で/解決）を隠す設定のときに出ていました）
- 実行トレースの表示中に解説の自動生成が完了すると、変数の値の上に解説文が重なって読めなくなる問題を直しました。トレース中は解説を出さず、「解説表示に戻る」で元通り出ます

## [0.21.1] - 2026-07-25

### 修正
- 実行トレースが「対象ファイルの実行に失敗: ModuleNotFoundError」で止まる問題を直しました。プロジェクト内の自分で書いたモジュールを読み込んでいるファイル（`from mypkg.models import ...` や `from . import ...` など）はトレースできない状態でしたが、対象ファイルを本来の場所で実行するようにしたため、普段そのファイルを動かすときと同じように読み込みが解決されます

## [0.21.0] - 2026-07-25

### 追加
- サイドバーに「トレース」タブを新設。実行トレースの起動・別の入力例での再実行・解説表示への切り替え・使い方の説明をここに集約しました（解説タブ下部のボタンは廃止）。タブ下部に現在のトレース状態（関数名・ループ数）を表示します
- 実行トレースの結果をキャッシュするようにしました。同じコードの同じ関数なら2回目以降は即表示（AI・実行なし・0円）。解説表示との行き来も一瞬になります。コードを編集すると自動で作り直します。「別の入力例で再実行」でキャッシュを無視して作り直せます

## [0.20.0] - 2026-07-24

### 追加
- **実行トレース**: カーソルを置いた関数を、AIが作った具体例の入力で実際に実行し、各行の右余白に変数の実際の値を表示します。サイドバー「解説」タブの「トレース実行」ボタン、または ⌥⌘T（Win/Linux: Ctrl+Alt+T）で起動します
  - 値はAIの推測ではなく、本当に実行した結果です（AIの役割は「入力の具体例づくり」「副作用の有無の事前チェック」「長い値の短縮表示の型紙づくり」の3つだけ）
  - ループは `◀ n周目/全m周 ▶` の周回表示付き。⌥⌘←/→、またはループ行にホバーして出る「前の周回/次の周回」リンクや周回一覧表で、見たい周を切り替えられます
  - 行の右の値が長くて切れているときは、その行にホバーすると全文が見えます
  - ファイル書き込みやネットワークに触る関数は実行せず、その旨を表示します。実行は一時フォルダ内・10秒タイムアウト付きです
  - インライン解説との切り替え: トレース表示中は解説を一時的に隠します。サイドバーの「解説表示に戻る」か ⌥⌘C で解説に戻り、もう一度見たければ「トレース実行」を押します。コードを編集するとトレースは自動で消えます

## [0.19.3] - 2026-07-22

### 変更
- ホバーに出る対象コード1行で、下線が引かれている部分の真下に `^` を敷いて位置を示すようにしました（行全体だけだと、どこの解説か分からなかったため）

## [0.19.2] - 2026-07-21

### 変更
- 下線（トークン）の解説をホバーで出したとき、解説の先頭に対象のコード1行を表示するようにしました。ホバーの吹き出しがコードに重なっても、何についての解説かが吹き出しの中だけで分かります

## [0.19.1] - 2026-07-21

### 修正
- 解説の生成が「解説を生成できませんでした」で終わってしまうことがある問題を軽減しました。AIの返答が途中で壊れていても、正しく届いている分だけを表示します（従来は1箇所でも壊れていると全部捨てていました）
- 上記が起きたとき、AIの返答そのものを「表示 > 出力 > AI Code Guide」に残すようにしました。原因が分かるようになります
- 解説が0件だったときのメッセージに、次に何を見ればよいか（出力パネル）を書くようにしました

## [0.19.0] - 2026-07-21

### 追加
- 下線（トークン）の解説の出し方に「ホバー」を追加。設定「下線の解説の位置」または解説タブの切り替えで、`行の下` / `右余白` / `ホバー` の3つから選べます。`ホバー` にすると点線下線だけが常に見え、解説はマウスを乗せたときに出ます（コードの行間が空かず、複数行を続けて読みたいときに向きます）。既定は従来どおり `行の下` です

### 変更
- 下線にホバーしたときの文を「それだけ読んで分かる説明」に作り直しました。従来は短い見出しと補足の2段だったため、見出しを見ずに補足だけ読むと話が途中から始まっていました。今後は1文目で「これが何か」、続けて「なぜこう書くか・注意点」を述べます（ブロック枠の解説は従来どおり）
- この変更に伴い、保存済みの解説は作り直しになります（同じファイルでももう一度生成されます）
- 重なったブロック解説の切り替えを、コード行の上に出る行から**ブロックのホバー内のリンク**へ移しました（「重なった説明 1/3 ・ 次を表示」）。コードの行間に余計な行が入らなくなります

### 修正
- 重なったブロックがある場所でコード本体にホバーすると、画面に表示されている解説ではなく、後ろに隠れている別の解説が出ることがあった問題を修正
- 下線の解説を「右余白」または「ホバー」にしているとき、ホバー内に「変更は行末のチップから」と案内が出るのに、そのチップが存在しなかった問題を修正（案内を出さないようにしました）

## [0.18.1] - 2026-07-20

### 修正
- フローチャート: ループ（for / while）の中身の最後のノードから、どこにも矢印が出ず行き止まりになっていた問題を修正。ループの先頭に戻る矢印を表示するようにしました（繰り返しの流れが図で追えるようになります）

## [0.18.0] - 2026-07-20

### 追加
- フローチャート: 単一関数の図（関数カードの「図」ボタン）に「文言: コード⇄日本語」の切り替えボタンを追加。既定は従来どおりコードそのままの文言、押すとAIが全ノードを短い日本語（条件分岐は「〜か?」の疑問形）に言い換えます。図の形・色・行ジャンプは変わりません。日本語文言は関数ごとに初回1回だけ生成し保存されます（コードを編集した関数だけ作り直し）

## [0.17.0] - 2026-07-20

### 変更
- 設計ファイル: 下書きの作り方を「上の層からの導出」に変更。ディレクトリ/ファイルの設計を書くとき、先にリポ全体・親ディレクトリの設計（特に開発者確認済み=@confirmedの「誰のどんな問題を解くか」「主要ユースケース」）を読み、そこから「あるべき目的・設計」を導いてコードと突き合わせる（コードの要約で目的欄を埋めない）
- 設計ファイル: 生成の最後に、AIが気づいた設計への疑問（責務の混在・ユースケースに無い複雑さ等）を「〜は意図的ですか？」の質問リストとしてチャットで開発者に投げるステップを追加

## [0.16.0] - 2026-07-20

### 追加
- 設計ファイル: 既存の設計ファイルを更新するプロンプトを追加。既存mdを埋め込み、開発者確認済み(@confirmed)の記述は維持したまま、AI推測(@inferred)・切れたanchor・不足セクションだけ書き直させる
- 設計が既にあるリポジトリ/ディレクトリ/ファイルの設計ブロックに「設計を更新」リンクを追加(コマンドパレットの「設計ファイル作成プロンプトをコピー」も既存設計があれば自動で更新プロンプトになります)

## [0.15.0] - 2026-07-20

### 変更
- 設計ファイル: 依頼プロンプトの手順を「開発者への白紙ヒアリング」から「AIが@inferredで全セクションを下書き→開発者が項目ごとに正誤判定→確定した項目だけ@confirmedへ書き換え」に変更。開発者は差分の指摘だけで済む

## [0.14.0] - 2026-07-20

### 追加
- 設計ファイル: リポ全体の依頼プロンプトで `_repo.md` 先頭に「誰のどんな問題を解くか」「主要ユースケース」の2セクションを必須化。ヒアリングで必ず開発者に確認する（@confirmed）よう指示し、AI推測（@inferred）での埋めを禁止

## [0.13.0] - 2026-07-16

### 追加
- **設計ファイル機能（フェーズ1）を追加しました**: コードとは別に「このコードは何のために・どういう方針で書かれたか」を記録する設計ファイル（`.ai-code-guide/design/` 配下のMarkdown）を読み込み、各タブに表示します。
  - **作る**: コマンド「AI Code Guide: 設計ファイル作成プロンプトをコピー」または各タブに出る「設計ファイルを作る」ボタンで、依頼プロンプトをコピーできます。Claude Code / codex に貼ると、あなたへのヒアリングを経て設計ファイルが作られます（拡張自体はAI生成しません。開発者が確認した内容 `確認済` と、AIがコードから推測した内容 `推測` をタグで区別して記録します）。
  - **見る**: プロジェクトタブ=リポジトリ全体・ディレクトリの設計とファイルカードの「設計」バッジ / 概要タブ=ファイルの目的 / 標準タブ=関数・クラスカードに目的・要件・制約・方針・構成の「設計」ブロック。すべての記述に出所バッジ（確認済=緑 / 推測=グレー / 未確認=橙）が付きます。
  - **検証**: 設計ファイルの書式違反や、コードに見つからない参照は警告として該当箇所に表示します。
  - 設計ファイルを編集すると、開いているタブへ自動で反映されます。

## [0.12.0] - 2026-07-16

### 変更
- **サイドバー上部のタブが、パネルの幅に合わせて自動で「⋯」メニューに折りたたまれるようになりました**: これまでは後半3つのタブが幅に関係なく常に「⋯」の中に隠れていました。今後はパネル幅に収まる分だけタブとして表示し、あふれた分だけ「⋯」メニューに入ります。パネルの幅を変えると自動で追従します。選択中のタブが「⋯」の中にある場合は、「⋯ 設定」のように現在地がボタンに表示されます。

## [0.11.1] - 2026-07-10

### 修正
- **ブロック解説の状態ボタン（読んだ／後で／解決）の位置ずれを直しました**: 複数行を囲むブロック解説に付く状態ボタンの行が、解説（サイドノート）の列とずれて表示されていました。上に下線の解説が無いブロックでは、ボタンがそのブロックのインデント分だけ右へずれていました（原点を列0に固定する処理が下線の行にしか効いておらず、ブロックの状態行に抜けていたため）。状態ボタンの行を解説の開始位置にそろえるよう修正しました。下線の解説と同じ行に重なるブロックでも、可能な限り解説の列へ寄せます（下線の解説が長い場合はその直後まで）。

## [0.11.0] - 2026-07-08

### 追加
- **チャットでコードを引用して質問すると、その箇所にAIの結論が解説として残るようになりました**: エディタでコードを選択して引用付きで質問すると、応答のあとにその引用箇所へ自動で解説が付きます。1行だけの引用は点線の下線、複数行にまたがる引用は枠で囲んで示します。表示される見出しは「会話の結論（＝この箇所が何をしているか）」で、会話を続けるたびに最新の結論へ更新されます。
  - 下線には短い一言の解説、枠にはやや詳しい解説と、囲みの大きさに合わせて長さを出し分けます。
  - 見出しにマウスを乗せると、より詳しい補足と「💬 会話へ」リンクが出て、元のチャットに戻れます。
  - コードを編集すると位置は自動で追従し、ファイルを開き直しても解説は復元されます。

## [0.10.3] - 2026-07-07

### 修正
- **「解説の位置」ボタンが表示ON/OFFのように見えていた問題を直しました**: 解説タブの「解説の位置」（下線の解説を「行の下」か「右余白」のどちらに出すか）が、他のON/OFFトグルと同じ塗りで表示され、「右余白」がまるで通常状態のように見えていました。左右に「行の下｜右余白」を並べて選択中だけ塗る切替に変え、2つから選ぶ設定だと一目で分かるようにしました。

## [0.10.2] - 2026-07-07

### 追加
- **インライン解説を「下線だけ」「ブロックだけ」で表示ON/OFFできるようになりました**: 解説タブの「解説を表示」の下に「下線の解説」「ブロックの解説」トグルを追加しました。トークンに付く点線下線の解説と、複数行を囲む枠の解説を、種類ごとに個別で隠せます。OFFにしても生成結果は残るのでONで即座に戻せます。2つのトグルは「解説を表示」の下位項目として字下げして表示します。

## [0.10.1] - 2026-07-07

### 修正
- **チャット履歴が見分けにくかった問題を直しました**: 履歴の各行が枠なしで並んでいて区切りが分かりづらかったのを、1件ずつ枠付きのカードにしました。今表示中のチャットは左端に色を付けて強調します。
- **チャット入力欄の存在感を上げました**: 入力欄の枠を太くし薄い影を付けて目立つようにしました。フォーカス時はさらに強調し、「新規チャット」を押した直後は入力欄が2回光って場所を知らせます。
- **省略タブの表示名を短くしました**: 「…」メニュー内の「プロジェクト」「インライン解説」が長くタブが窮屈だったため、それぞれ「構成」「解説」に短縮しました（ホバーで元の名前を確認できます）。

## [0.10.0] - 2026-07-07

### 追加
- **タブバーが1段にまとまりました**: サイドバー上部のタブが2段に折り返して場所を取っていたのを、よく使うタブだけを1段に並べる形に整理しました。使用量・設定・ヘルプは右端の「…」メニューにまとめ、必要なときだけ開けます。
- **インラインの状態ボタンを表示するか選べるようになりました**: インライン解説につく「読んだ／後で／解決／隠す」のボタンを、設定でまとめて表示ON/OFFできるようにしました。ボタンが邪魔なときは隠せます。あわせてインライン解説の操作を独立した「インライン」タブに分けて、探しやすくしました。

### 修正
- **インライン解説タブの表示が整いました**: タブ内のボタンを縦1列に並べてそれぞれに説明を添え、何をするボタンか分かりやすくしました。入力欄のプレースホルダが2行に折り返して崩れていたのも1行に固定しています。
- **説明つきセレクトの表示崩れを直しました**: 密度（解説の量）を選ぶセレクトが、全体スタイルの影響で横に潰れて説明文が読めなくなっていたのを直しました。

## [0.9.1] - 2026-07-07

### 修正
- **下線の解説（↑）が正しく下線の真下に揃うようになりました**: 点線下線の解説行の ↑ が、行によって右や左にバラバラにズレて見える問題を直しました。ズレ量に法則が見えなかった原因は、VS Code が解説行を「下の行のインデント位置」に置いたり「左端」に置いたりを行の長さで切り替えていたためで、常に左端起点になるよう固定しました。タブや全角文字を含む行でも揃います。

## [0.9.0] - 2026-07-06

### 追加
- **チャットの返信を途中で止められるようになりました**: 質問を送るとボタンが「送信」から「中止」に変わり、押すと待っている返信をその場で打ち切れます（返信は「（中止しました）」と表示され、送信待ちにためていたメッセージも取り消されます）。質問を間違えた・的外れな答えが来そう、というときに最後まで待たずにやり直せます。
  - ※これは「待つのをやめる」操作です。すでに生成が始まった分のトークン節約を保証するものではありません。

## [0.8.2] - 2026-07-06

### 修正
- **▶展開トグルが押しやすくなりました**: 標準ビューのメインカード（関数・処理ブロック）にある▶／▼の展開ボタンが小さくて押しづらかったのを直しました。見た目の大きさやカードの行の高さは変えずに、クリックできる範囲（当たり判定）だけを広げています。

## [0.8.1] - 2026-07-06

### 修正
- **▶展開したメインカードの色が目に優しくなりました**: カードを▶で展開したとき、上のメインカード（見出し）の背景が明るすぎて（青が強すぎて）目が疲れる問題を直しました。展開グループのメイン・概要・サブカードが同じ落ち着いたトーンで揃い、境目はカードの枠線と親色の継ぎ目で分かるようになっています。

## [0.8.0] - 2026-07-06

### 追加
- **チャットの引用が文中に入るようになりました**: エディタでコードを選択してコピーし、チャットの入力欄に貼り付けると、生のコードの代わりに**引用チップ（`引用1 ファイル名 L83 「コード先頭」`のような小さなタグ）がカーソル位置に**入ります。チップの前後にそのまま文章を書けるので、「こっちのやつ(引用1)はいいけど、こっちのやつ(引用2)は直して」のように**複数の引用を文中で指し分けて**質問できます（AIにも番号付きで正しく伝わります）。チップは×ボタンでもBackspaceでも外せて、残った引用の番号は自動で振り直されます。送信後の自分の吹き出しでも、引用は文中のその位置にチップとして表示されます。
- **AIの返信中でも次のメッセージを送れるようになりました**: 返信待ちの間も入力・送信ができ、送った分は「あなた（送信待ち）」として薄く表示され、返信が終わると自動で順番に送信されます。返信が来た瞬間に入力途中の文章が消えることもなくなりました。
- **カードの▶展開に「概要」が付きました**: 関数カードを▶で展開すると、サブカード（処理の分解）の前に**目的・入力・出力（・補足）**の説明が色付きタグで表示されます。「この関数が何のためにあるか」「何を受け取って何を返すか」が先に分かるので、上から読むときの前提が掴めます（補足には「先に既知IDを取得しているのは重複チェックのため」のような非自明な背景が入ります）。

### 変更
- **チャットの吹き出しを色分けしました**: 自分=青系、AI=グレー系で見分けやすくなりました。
- **モジュールマップ（標準タブ）のレイアウトを刷新しました**: カードがサイドバーの端まで使う全幅表示になりました。▶展開したグループは「メインカード=濃い親色 / 概要・サブカード=薄い親色 / カードの継ぎ目と下端=親色の帯」という同じ色の濃淡で表示され、どのカードがどの関数の配下かが色で追えます。サブカードの左インデントは廃止し、各カードには細い枠線で輪郭を付けています。

### 修正
- **インライン説明の「↑」の位置ズレを直しました**: コード下の説明（↑付きのCodeLens）の矢印が、指すべきトークンの下線から右や左に数文字ズレることがありました。位置計算を実測に基づいて補正し、下線の真下を指すようになりました。

## [0.7.2] - 2026-07-06

### 修正
- **▼展開サブブロックの背景色が文の途中で切れなくなりました**: モジュールマップ（標準タブ）でカードを▼展開したとき、サブブロックの色分けが複数行の文（複数行にわたる関数呼び出しなど）の途中で切れたり、どの色にも塗られない行が残ったりすることがありました。AIが提案した行の区切りをそのまま使わず、**Python の文法上の文の区切りに機械的に合わせ直す検証層**を追加したため、色の境界は必ず文の切れ目になり、隙間もなくなります。あわせてラベルがブロック内の一部の処理だけを名乗らないよう生成ルールを改善しました（既存の展開結果は作り直されます）。

## [0.7.1] - 2026-07-04

### 修正
- **カードのAI説明が無駄に作り直されなくなりました**: モジュールマップ（標準・概要タブの関数／クラスのカード）でAI説明を付けたあと、ファイルのどこか一箇所を直して「AI説明」を押し直すと、これまでは**関係のないカードまで全部**作り直され（＝そのぶんトークンを消費し）ていました。今回から、**中身が変わったカードだけ**が作り直され、変えていないカードは前の説明をそのまま再利用します。ファイルの先頭に関数を足すなど、位置が大きくずれる編集でも同じく再利用されます（インライン説明が部分編集に強いのと同じ考え方に揃えました）。上部のファイル全体の見出しも、関数の中身をいじっただけでは作り直されず、関数・クラスの構成が変わったときだけ更新されます。

## [0.7.0] - 2026-07-04

### 追加
- **フローチャートが編集に自動追従**: 標準フローチャート（モジュールマップ）を表示したまま、そのファイルを編集すると、**保存やタブの切り替えをしなくても**約0.5秒後に自動で最新の内容へ描き直されるようになりました。自分でコードを打ったときも、Claude Code など外部ツールがファイルを書き換えたときも同じように追従します。これまでは編集してもフローチャートが古いまま残り、行の背景色ハイライトがずれることがありましたが、その状態が解消されます（AIの再生成は走らないためトークンは消費しません）。

## [0.6.0] - 2026-07-04

### 追加
- **チャットのモデル表示・切替**: チャット入力欄のすぐ上に、いま会話に使っているモデル（例: `Claude Sonnet（サブスク）`）を常に表示します。バッジをクリックすると切り替えられます。**選べるのは今の認証で実際に使えるモデルだけ**です（Claude サブスク利用中なら Claude のみ、API キーを登録したプロバイダのモデルだけが並びます。使えない GPT/Gemini などは表示されません）。
- **チャットの「思考の深さ（effort）」切替**: モデルの隣のバッジで、AIがどれだけ深く考えるかを `low / medium / high` から選べます。**effort（エフォート）＝ 思考にかける手間**のことで、深いほど賢い代わりに遅く・コストが高くなります。既定は `low`（従来どおり高速）。表示名は選んだモデルに合わせて変わります（Claude=`effort` / GPT=`reasoning` / Gemini=`thinking`）。プロバイダごとに指定方法が違いますが、アプリ側で吸収します。effort に対応しないモデル（Claude Haiku 4.5 など）では無視されます。
- **インライン説明のキャッシュ自動復元**: 一度AI説明を生成したファイルは、閉じて開き直すと（自動生成をOFFにしていても）保存済みの説明がすぐ再表示されます。トークンは消費しません。

### 変更
- **引用チップを入力欄の中へ（Cursor風）**: 引用したコードの場所参照（例: `algorithms.py L19-24`）を、入力欄の中の上部にコンパクトなチップとして表示するようにしました。複数の箇所を同時に引用でき、それぞれ `×` で個別に外せます。従来の「引用」ラベルや「コードを表示」トグルは省き、場所だけをすっきり示します。

## [0.5.1] - 2026-07-04

### 修正
- **チャット入力欄の見切れ**: チャットタブ上部の注釈ツールバーがボタン増加で複数行に折り返した際、下部の入力欄が数px見切れることがある問題を修正しました。上部（ツールバー＋履歴）を、高さが足りないときだけ内部スクロールで縮む領域にまとめ、入力欄が常に表示されるようにしました（通常の高さでは見た目は変わりません）。
- **ツールバーが上部に固定されるように**: 標準・概要・プロジェクトタブで、下にスクロールすると「矢印」「色」「▼全て」「範囲展開」などのボタンが画面外に流れて、押すたびに上へ戻る必要があった問題を修正しました。ツールバーをタブ上部に貼り付け、スクロールしても常に表示されるようにしました。

## [0.5.0] - 2026-07-04

### 追加
- **選択コードを貼り付けて引用**: エディタでコードをコピーし、チャット欄にそのまま貼り付けると、生のコードではなく `引用 ファイル名 L11-20` の場所参照チップに自動変換されます（直前にエディタで選択した範囲と一致したときのみ。一致しなければ通常の貼り付け）。ショートカット Cmd+Alt+Q や右クリックからの引用も引き続き使えます。

## [0.4.0] - 2026-07-04

### 追加
- **選択範囲をチャットに引用**: エディタでコードを範囲選択し、Cmd+Alt+Q（Win/Linux: Ctrl+Alt+Q）または右クリック「選択範囲をチャットに引用」で、チャットに `引用 ファイル名 L11-20` の場所参照チップが付きます。生コードを貼り付けず、どのファイルのどこかを保ったまま質問できます（コードはチップ内で展開して確認可、× で外せます）。送信するとその箇所を文脈にしてAIが回答します。

### 変更
- **サブカードの行番号表示を削除**: モジュールマップのカードを▶で展開したときのサブカードから `Lxx–yy` の行番号表示を外し、見出しと説明だけのすっきり表示にしました（クリックでの行ジャンプは従来どおり動きます）。

## [0.3.0] - 2026-07-04

### 追加
- **モジュールマップのAI説明を独立行で表示**: 関数カードの見出しは関数名のみにして、AI説明はその下の行にまとめて表示するようにした（クラスと体裁を統一）。
- **単一関数のフロー図に拡大縮小**: 「図」を開いた画面に −／＋／リセットのズーム操作を追加。大きな図も見やすくなった。
- **下線解説の表示切り替え**: 「下線: ON/OFF」で全解説をまとめて非表示／再表示できる（生成結果は保持され即座に戻せる）。各解説を個別に「隠す」／「隠した注釈も表示」で戻す操作も追加。
- **カードの範囲展開**: ツールバーの「範囲展開」からモードに入り、カードの範囲をクリックで選んでまとめてAI分解できるようにした。

### 修正
- **下線解説の位置**: 下線（symbol）の解説ボタンが下線から左にずれて表示される問題を修正し、下線の真下に来るようにした。
- **フロー図のエラー**: 「図」を開いた状態からチャットして標準表示に戻ると、フロー図が「Syntax error」になる問題を修正。
- **AI説明が出ないことがある問題**: 構造系モデルに Haiku 4.5 などを使うと、モジュールマップのカードにAI説明が生成されない問題を修正（モデル応答からの取り出しを頑健にした）。

## [0.2.2] - 2026-07-03

### 修正
- **標準タブのモジュールマップ**で、連続するトップレベルの変数定義が1つずつ別々のカードに分割されていた問題を修正。import と同じように連続する定義を1枚のカードに束ねるようにした（見出しは変数名を列挙）。

## [0.2.0] - 2026-07-03

### 追加
- **AI編集検知 → 提案式インライン解説**: AIによる一括編集を検知し、その範囲に絞ってインライン解説を提案する機能を追加。
  - 検知ロジック `bulkEditDetector`（純粋関数・単体テスト付き）
  - 動作モード設定 `off` / `suggest` / `auto`（既定は提案式）
  - ハイライト＋CodeLens による提案UI、検知範囲に絞った解説生成

### 変更
- ヘルプタブをランチャー＋クイックリファレンスに再設計、全UIから装飾絵文字を削除。
- APIキーの保存先を OS キーチェーン（SecretStorage）に移行済みの説明・削除/入れ替え手順をヘルプへ整理。

### ドキュメント
- 宣伝用デモGIF・ショーケース資料（`docs/showcase.md`）を追加。

## [0.1.2] - 2026-07-02

### 変更
- APIキー保存を SecretStorage（OSキーチェーン）に移行。設定コマンド追加・既存 settings からの自動移行に対応。
